{% if settings.homepage_slideshow %}
    <section class="home-slider pv4">
      {% if settings.homepage_slide_show_1 %}
      <article class="relative" style="height:auto">
        <a href="{{ settings.homepage_slide_link_1 }}" class="link near-white {{ settings.homepage_slide_overlay_1 }}">
            <img src="{{ 'homepage_slide_image_1.jpg' | asset_url }}" alt="{{ settings.homepage_slide_title_1 }}"/>
            <div class="absolute measure lh-title slide-text-{{ settings.homepage_slide_text_1_position }} {{ settings.homepage_slide_text_1_align }}" style="color: {{ settings.homepage_slide_1_colour }}">
                <h1 class="f3 f2-l ma0">{{ settings.homepage_slide_title_1 }}</h1>
                <h2 class="f5 f4-l i ma0 mt2">{{ settings.homepage_slide_text_1 }}</h2>
            </div>
        </a>
      </article>
      {% endif %}
      {% if settings.homepage_slide_show_2 %}
      <article class="relative" style="height:auto">
        <a href="{{ settings.homepage_slide_link_2 }}" class="link near-white {{ settings.homepage_slide_overlay_2 }}">
            <img src="{{ 'homepage_slide_image_2.jpg' | asset_url }}" alt="{{ settings.homepage_slide_title_2 }}"/>
            <div class="absolute measure lh-title slide-text-{{ settings.homepage_slide_text_2_position }} {{ settings.homepage_slide_text_2_align }}" style="color: {{ settings.homepage_slide_2_colour }}">
                <h1 class="f3 f2-l ma0">{{ settings.homepage_slide_title_2 }}</h1>
                <h2 class="f5 f4-l i ma0 mt2">{{ settings.homepage_slide_text_2 }}</h2>
            </div>
        </a>
      </article>
      {% endif %}
      {% if settings.homepage_slide_show_3 %}
      <article class="relative" style="height:auto">
        <a href="{{ settings.homepage_slide_link_3 }}" class="link near-white {{ settings.homepage_slide_overlay_3 }}">
            <img src="{{ 'homepage_slide_image_3.jpg' | asset_url }}" alt="{{ settings.homepage_slide_title_3 }}"/>
            <div class="absolute measure lh-title slide-text-{{ settings.homepage_slide_text_3_position }} {{ settings.homepage_slide_text_3_align }}" style="color: {{ settings.homepage_slide_3_colour }}">
                <h1 class="f3 f2-l ma0">{{ settings.homepage_slide_title_3 }}</h1>
                <h2 class="f5 f4-l i ma0 mt2">{{ settings.homepage_slide_text_3 }}</h2>
            </div>
        </a>
      </article>
      {% endif %}
      {% if settings.homepage_slide_show_4 %}
      <article class="relative" style="height:auto">
        <a href="{{ settings.homepage_slide_link_4 }}" class="link near-white {{ settings.homepage_slide_overlay_4 }}">
            <img src="{{ 'homepage_slide_image_4.jpg' | asset_url }}" alt="{{ settings.homepage_slide_title_4 }}"/>
            <div class="absolute measure lh-title slide-text-{{ settings.homepage_slide_text_4_position }} {{ settings.homepage_slide_text_4_align }}" style="color: {{ settings.homepage_slide_4_colour }}">
                <h1 class="f3 f2-l ma0">{{ settings.homepage_slide_title_4 }}</h1>
                <h2 class="f5 f4-l i ma0 mt2">{{ settings.homepage_slide_text_4 }}</h2>
            </div>
        </a>
      </article>
      {% endif %}
      {% if settings.homepage_slide_show_5 %}
      <article class="relative" style="height:auto">
        <a href="{{ settings.homepage_slide_link_5 }}" class="link near-white {{ settings.homepage_slide_overlay_5 }}">
            <img src="{{ 'homepage_slide_image_5.jpg' | asset_url }}" alt="{{ settings.homepage_slide_title_5 }}"/>
            <div class="absolute measure lh-title slide-text-{{ settings.homepage_slide_text_5_position }} {{ settings.homepage_slide_text_5_align }}" style="color: {{ settings.homepage_slide_5_colour }}">
                <h1 class="f3 f2-l ma0">{{ settings.homepage_slide_title_5 }}</h1>
                <h2 class="f5 f4-l i ma0 mt2">{{ settings.homepage_slide_text_5 }}</h2>
            </div>
        </a>
      </article>
      {% endif %}
  </section>
{% endif %}

{% if collections.home-page %}
<section class="partition mv5">
    <header class="bb bw1 b--dbc-orange mb3">
        <h1 class="f3 f2-ns ma0 lh-title">Smart Working</h1>
    </header>
    <div class="cf" style="margin: 0 -1rem;">
      {% for product in collections.smart-working.products %}
        <article class="fl w-50 w-third-m w-20-l pa3 border-box {{ product.metafields.attribute.type | handleize }}{% unless product.available %} sold-out{% endunless %}">
            <a href="{{ product.url | within: collection }}" title="{{ product.title }}">
                <figure class="ma0">
                    <img src="{{ product.featured_image | product_img_url: 'large' }}" alt="{{ image.alt | escape }}" data-at2x="{{ product.featured_image | product_img_url: 'grande' }}">
                </figure>
            </a>
        </article>
      {% endfor %}
    </div>
  </section>
 {% endif %}
 {% if collections.home-page %}
 <section class="partition mv5">
     <header class="bb bw1 b--dbc-orange mb3">
         <h1 class="f3 f2-ns ma0 lh-title">Slow Living</h1>
     </header>
     <div class="cf" style="margin: 0 -1rem;">
         {% for product in collections.slow-living.products %}
         <article class="fl w-50 w-third-m w-20-l pa3 border-box {{ product.metafields.attribute.type | handleize }}{% unless product.available %} sold-out{% endunless %}">
             <a href="{{ product.url | within: collection }}" title="{{ product.title }}">
                 <figure class="ma0">
                     <img src="{{ product.featured_image | product_img_url: 'large' }}" alt="{{ image.alt | escape }}" data-at2x="{{ product.featured_image | product_img_url: 'grande' }}">
                 </figure>
             </a>
         </article>
       {% endfor %}
     </div>
   </section>
  {% endif %}

<!-- <ul id="newBooks" class="partition cf">
        <header class="cf">
            <hgroup class="wrapper">
                <h1 class="section_title">Books that make it easier to&nbsp;Do&nbsp;stuff</h1>
            </hgroup>
        </header>
        <div class="equalheights">
    {% if collections.home-page %}
      {% for product in collections.guidebooks.products %}
        <li class="bookPreview wrapper {{ product.metafields.attribute.type | handleize }}{% unless product.available %} sold-out{% endunless %}">
            <a href="{{ product.url | within: collection }}" title="{{ product.title }}">
                <figure class="cf">
                    <img src="{{ product.featured_image | product_img_url: 'large' }}" alt="{{ image.alt | escape }}" data-at2x="{{ product.featured_image | product_img_url: 'grande' }}">
                    {% if product.tags contains 'new' or product.tags contains 'New' %}
                        <span class="button">New</span>
                    {% else %}
                        <span class="button">Learn more</span>
                    {% endif %}
                </figure>
            </a>
        </li>
        {% if forloop.index == 4 %}
            <li class="bookPreview wrapper ebook_notice">
              <p>NEW: Free digital copy with each paperback purchase!</p>
            </li>
        {% endif %}
      {% endfor %}
      </div>
    </ul>
     {% endif %}
            {% if collections.bundles %}
              {% for product in collections.bundles.products %}
                <form class="addtocart pa4 bg-dbc-washed-orange br2 ba bw1 b--dbc-orange tc" action="/cart/add" method="post">
                  {% comment %} Return to product instead of going to cart {% endcomment %}
                  <input type="hidden" name="return_to" value="back" />
                    <header>
                        <h2>{{collections.bundles.description}}</h2>
                    </header>
                    {% for variant in product.variants %}
                      {% if variant.available == true %}
                        <input type="radio" name="id" id="format_{{variant.id}}" value="{{ variant.id }}" class="dn"/>
                        <label for="format_{{variant.id}}" class="radio-button-label dib myriad fw6 input-reset br2 ba bw1 bg-dbc-washed-orange ttu pv2 ph4 tracked dbc-orange">{% if variant.title == "Bundle" %}Add pb + ebook set{% else %}Add {{ variant.title }} set{% endif %} {{ variant.price | money | remove: '.00' }} <span class="db mt2 i caslon fw4 ttn" style="letter-spacing: 0;">14 books</span></label>
                        {% else %}
                        Out of stock
                      {% endif %}
                     {% endfor %}
                   {% unless product.available %}
                    <p class="coming_soon wrapper"><em>~ Available from 1st May 2014 ~</em></p>
                   {% endunless %}
                    <input type="submit" class="dn submit" value="Add to Basket" id="add" name="add" />
                </form>
          {% endfor %}
         {% endif %}-->

<!-- <ul id="newBooks" class="partition cf">
        <header class="cf">
            <hgroup class="wrapper">
              <h1 class="section_title">Gift ideas <span class="callout"></span></h1>
            </hgroup>
        </header>
        <div class="gift-bundles equalheights">
    {% if collections.home-page %}
      {% for product in collections.gift-bundles.products %}
        <li style="" class="bookPreview wrapper {{ product.type | handleize }} {{ product.metafields.attribute.type | handleize }}{% unless product.available %} sold-out{% endunless %}">
            <a href="{{ product.url | within: collection }}" title="{{ product.title }}">
                <figure class="cf">
                    <img src="{{ product.featured_image | product_img_url: 'large' }}" alt="{{ image.alt | escape }}" data-at2x="{{ product.featured_image | product_img_url: 'grande' }}">
                    <h4>{{ product.title }}</h4>
                </figure>
            </a>
        </li>
      {% endfor %}
      </div>
    </ul>
     {% endif %} -->

        {% if collections.christmas %}
          {% for product in collections.christmas.products %}
            <form class="christmas addtocart cf" action="/cart/add" method="post">
              {% comment %} Return to product instead of going to cart {% endcomment %}
              <input type="hidden" name="return_to" value="back" />
                <header>
                    <h2>{{ collections.christmas.description }}</h2>
                </header>
                <select id="product-select" name="id">
                {% for variant in product.variants %}
                    <option value="{{ variant.id }}">{{ variant.title }} - {{ variant.price | money }}</option>
                {% endfor %}
                </select>
                <input type="submit" class="submit button" value="Add to Basket" id="add" name="add" />
                <script>
                // <![CDATA[
                var selectCallback = function(variant, selector) {
                  if (variant) {
                    if (variant.available) {
                      // Selected a valid variant that is available.
                      $('#add').removeClass('disabled').removeAttr('disabled').val('Add to Cart').fadeTo(200,1);
                    } else {
                      // Variant is sold out.
                      $('#add').val('Sold Out').addClass('disabled').attr('disabled', 'disabled').fadeTo(200,0.5);
                    }
                    // Whether the variant is in stock or not, we can update the price and compare at price.
                    if ( variant.compare_at_price > variant.price ) {
                      $('#price-field').html('<span class="product-price on-sale">'+ Shopify.formatMoney(variant.price, "{{ shop.money_format }}") +'</span>'+'&nbsp;<s class="product-compare-price">'+Shopify.formatMoney(variant.compare_at_price, "{{ shop.money_format }}")+ '</s>');
                    } else {
                      $('#price-field').html('<span class="product-price">'+ Shopify.formatMoney(variant.price, "{{ shop.money_format }}") + '</span>' );
                    }
                  } else {
                    // variant doesn't exist.
                    $('#add').val('Unavailable').addClass('disabled').attr('disabled', 'disabled').fadeTo(200,0.5);
                  }
                }
                // initialize multi selector for product
                jQuery(function($) {
                  new Shopify.OptionSelectors("product-select", { product: {{ product | json }}, onVariantSelected: selectCallback });
                });
                // ]]>
                </script>
            </form>
          {% endfor %}
         {% endif %}

        <section class="partition mv5">
            <div class="cf" style="margin: 0 -1rem;">
                <article class="fl w-100 w-third-ns tc pa3 border-box relative bg-near-black">
                    <div class="aspect-ratio aspect-ratio--1x1 mb4">
                        <div class="aspect-ratio--object cover z-0 o-90" style="background:url(https://d3uepj124s5rcx.cloudfront.net/items/2y1d2U2w03431S0Q1S1f/Whosit-&-Whatsit-DoBooks-.jpg) center;"></div>
                        <header class="absolute left-0 right-0 absolute-vertical-align near-white z-1">
                            <h2 class="f3 ma0">Events</h2>
                            <p>See our upcoming events »</p>
                        </header>
                    </div>
                </article>
                <article class="fl w-100 w-third-ns tc pa3 border-box relative bg-near-black">
                    <div class="aspect-ratio aspect-ratio--1x1 mb4">
                        <div class="aspect-ratio--object cover z-0 o-90" style="background:url(https://d3uepj124s5rcx.cloudfront.net/items/2y1d2U2w03431S0Q1S1f/Whosit-&-Whatsit-DoBooks-.jpg) center;"></div>
                        <header class="absolute left-0 right-0 absolute-vertical-align near-white z-1">
                            <h2 class="f3 ma0">About</h2>
                            <p>See our upcoming events »</p>
                        </header>
                    </div>
                </article>
                <article class="fl w-100 w-third-ns tc pa3 border-box relative bg-near-black">
                    <div class="aspect-ratio aspect-ratio--1x1 mb4">
                        <div class="aspect-ratio--object cover z-0 o-90" style="background:url(https://d3uepj124s5rcx.cloudfront.net/items/2y1d2U2w03431S0Q1S1f/Whosit-&-Whatsit-DoBooks-.jpg) center;"></div>
                        <header class="absolute left-0 right-0 absolute-vertical-align near-white z-1">
                            <h2 class="f3 ma0">Stockists</h2>
                        </header>
                    </div>
                </article>
            </div>
        </section>
<!-- <section id="recently" class="partition cf">
    <header class="wrapper">
        <h2 class="section_title">Recently at Do&nbsp;books</h2>
    </header>
    <ul class="cf">
        {% for article in blogs.news.articles limit:1 %}
        <li class="cf">
            <article>
                <figure class="wrapper">
                    <a href="{{ article.url }}">
                    {% if article.excerpt != blank %}
                        {{ article.excerpt }}
                    {% else %}
                        {% capture article_img %}{{ article.content | truncatewords: 3 }}{% endcapture %}
                        {% if article_img contains "img" %}
                            {% include article_img %}
                        {% endif %}
                    {% endif %}
                    </a>
                </figure>
                <div class="words wrapper">
                    <header>
                        <h1 class="blog-title"><a href="{{article.url}}">{{ article.title }}</a></h1>
                    </header>
                    <div class="article-body cf">
                        {{ article.content | strip_html | truncatewords: 48 }}
                        <p><a href="{{article.url}}" title="Continue reading {{ article.title }}">Continue reading &raquo;</a></p>
                      </div>
                </div>
            </article>
        </li>
        {% endfor %}
    </ul>
</section>
<aside id="miniAbout" class="partition cf">
    <header class="wrapper">
        <h2 class="section_title">Who we&nbsp;are</h2>
    </header>
    <figure class="wrapper">
        <img src="//cdn.shopify.com/s/files/1/0176/9202/files/559205_449980518392636_1109027229_n_grande.jpg" />
    </figure>
    <div class="wrapper words">
        <header>
            <h1>The Do Book&nbsp;Company</h1>
        </header>
        <p>The Do Book Company is an independent publishing house based in Shoreditch, London. We publish a series of inspirational pocket guidebooks in print and digital&nbsp;formats.</p>
        <p>You can find us on <a href="http://twitter.com/dobookco">Twitter</a>, <a href="https://www.facebook.com/DoBookCo">Facebook</a>, or you can <a href="/pages/subscribe">subscribe to our&nbsp;newsletter</a>.</p>
        <p><a href="/pages/about-us">Learn more about us&nbsp;&raquo;</a></p>
    </div>
</aside> -->
